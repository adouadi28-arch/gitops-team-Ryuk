apiVersion: v1
kind: Service
metadata:
  name: todo-api-canary
  namespace: todo-api-canary
spec:
  type: ClusterIP
  selector:
    app: todo-api-canary
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
